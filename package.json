{
  "name": "preentrega2darosayonatan",
  "version": "1.0.0",
  "description": "packaje.json",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1", 
    "build-css": "node-sass --include-path scss estilos/scss/estilo.scss estilos/css/style.css",
    "watch-css": "nodemon -e scss -x \"npm run build-css\""
   
  },
  "author": "yonatan",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^8.0.0",
    "nodemon": "^2.0.21"
  }
}
